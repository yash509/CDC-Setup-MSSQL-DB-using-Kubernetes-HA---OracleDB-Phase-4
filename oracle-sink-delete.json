The below will be used through the Postman for deleting the currect pipeline of the Oracle DB connector i.e., "oracle-sink" as a "DELETE" method in the Postman 
URL = "http://localhost:8083/connectors/oracle-sink"

The deletion of the pipeline of the whole "oracle-sink" connector will be done through the following URL only "http://localhost:8083/connectors/oracle-sink" by hitting it on the Postman
